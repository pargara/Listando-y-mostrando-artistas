<div class="container">
  <div class="row">
    <% @artist.each do |artist|  %>
    <div class="col-md-3">
      <div class="card mb-4 shadow-sm">
        <img class="card-img-top card-img-artist" src=<%= artist.image_url %>>
        <div class="card-body">
          <h4 class="card-title"><%= artist.name %></h4>
          <%= link_to "Ver más", artist_path(artist.id), class: "btn btn-primary"  %>
        </div>
      </div>
    </div>
    <% end %>

  </div>
</div>
